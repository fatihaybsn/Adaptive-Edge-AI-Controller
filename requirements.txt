# Jetson-focused pip dependencies for Adaptive Edge-Inference Controller.
#
# Install JetPack / platform packages first:
# - OpenCV must come from JetPack/L4T so cv2 keeps GStreamer support.
# - PyTorch and torchvision must match the installed JetPack/CUDA version.
#
# Do not add opencv-python, opencv-contrib-python, opencv-python-headless,
# torch, or torchvision here.
#
# Recommended Jetson install order:
#   python3 -m pip install -r requirements.txt
#   python3 -m pip install --no-deps ultralytics==8.4.53 ultralytics-thop==2.0.18

PyYAML>=5.3.1
numpy>=1.23.0,<2.0
scikit-fuzzy>=0.4.2
psutil>=5.8.0

# Ultralytics/runtime support dependencies that are safe to resolve from PyPI.
# Ultralytics itself is installed separately with --no-deps to avoid pulling
# opencv-python on Jetson.
matplotlib>=3.3.0
pillow>=7.1.2
requests>=2.23.0
scipy>=1.4.1
tqdm>=4.64.0
polars>=0.20.0

# Offline experiment and FOPDT identification scripts.
pandas>=1.3.0
